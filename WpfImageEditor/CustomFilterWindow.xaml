<Window x:Class="WpfImageEditor.CustomFilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Niestandardowy filtr (wagi kernela)" Height="380" Width="420"
        ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Rozmiar kernela:" VerticalAlignment="Center"/>
            <ComboBox x:Name="CmbSize" Width="70" SelectionChanged="CmbSize_Changed">
                <ComboBoxItem Content="3x3" IsSelected="True"/>
                <ComboBoxItem Content="5x5"/>
                <ComboBoxItem Content="7x7"/>
            </ComboBox>
        </StackPanel>

        <Label Grid.Row="1" Content="Wpisz wagi (wiersze oddzielone Enter, wartosci spacjami):"
               FontSize="10"/>

        <TextBox x:Name="TxtKernel" Grid.Row="2" AcceptsReturn="True"
                 FontFamily="Consolas" FontSize="13"
                 VerticalScrollBarVisibility="Auto"
                 Margin="0,4"
                 Text="0 -1 0&#x0a;-1 5 -1&#x0a;0 -1 0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Zastosuj" Width="90" Click="Apply_Click"/>
            <Button Content="Anuluj"   Width="90" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
